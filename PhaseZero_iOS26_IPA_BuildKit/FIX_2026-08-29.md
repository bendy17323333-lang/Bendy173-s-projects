# Build fix

This revision splits the shield-ring SwiftUI expression in
`NativeCombatFXView.swift` into a dedicated `ShieldRing` view. This avoids the
Xcode 26 compiler timeout reported at the old line 147:

`the compiler is unable to type-check this expression in reasonable time`

The bundled GitHub Actions workflow also uploads the build log even when the
build fails.
